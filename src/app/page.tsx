import { Button, Container } from "@mui/material";

const page = () => {
  return (
    <div>
      <Container>
        <h1>Home page</h1>
        <Button>Contained</Button>
      </Container>
    </div>
  );
};

export default page;
